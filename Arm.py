import wpilibfrom Globals import *from RobotSystem import *class RobotArm:    def __init__(self):        self.armTimer = wpilib.Timer()        self.armTimer.start()    def Raise(self):        robot.armUp.set(True)        robot.armDown.set(False)        self.armTimer.reset()    def Lower(self):        robot.armUp.set(False)        robot.armDown.set(True)        self.armTimer.reset()            def Reset(self):        robot.armUp.set(False)        robot.armDown.set(False)            def OperatorControl(self):        if armRaiseControl = True:            self.Raise()                    if armLowerControl = True:            self.Lower()                    if self.armTimer.Get() > .1:            self.Reset()